# Kubernetes

Field notes on running and debugging Kubernetes clusters: etcd, performance and eviction tuning, the kube-apiserver `goaway` parameter, running multiple etcd instances, and the kubectl/debugging commands I keep coming back to.
